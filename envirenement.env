# 🔹 REMPLACE PAR TON DOMAINE COOLIFY
N8N_DOMAIN=n8n.dbyway.com
MCP_DOMAIN=mcp.dbyway.com

# Ports internes (ne pas changer)
N8N_PORT=5678
MCP_PORT=8080

# Configuration n8n
N8N_PROTOCOL=https
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=tech
N8N_BASIC_AUTH_PASSWORD=AZER2025@n8n  # 🔹 Change moi !

# Timezone
GENERIC_TIMEZONE=Europe/Berlin  # 🔹 Change selon ta zone
TZ=Europe/Berlin

# MCP Configuration
MCP_CONNECTION_TIMEOUT=60000
MCP_RETRY_ATTEMPTS=3
